{
  "name": "invoicechaser",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @invoicechaser/mcp-server && npm run build -w @invoicechaser/web",
    "dev": "npm run dev -w @invoicechaser/web",
    "typecheck": "npm run typecheck -w @invoicechaser/mcp-server && npm run typecheck -w @invoicechaser/web"
  }
}
